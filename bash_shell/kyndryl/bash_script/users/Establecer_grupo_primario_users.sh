#!/bin/bash

# === SystemP ===
usermod -g users kyndjlfm
usermod -g users kyndjcoc
usermod -g users kyndjjac
usermod -g users kyndjdpr
usermod -g users kyndtssc
usermod -g users kyndajue
usermod -g users kyndjdar
usermod -g users kyndjvpp
usermod -g users kynddrpg
usermod -g users kyndcacc
usermod -g users kyndbsdc
usermod -g users kyndascy
usermod -g users kyndaype

# === Backups ===
usermod -g users kyndfmmf
usermod -g users kyndorcd
usermod -g users kyndjvpp
usermod -g users kyndcacc

# === Data Management ===
usermod -g users kyndrcgd
usermod -g users kyndflgi
usermod -g users kyndjalp
usermod -g users kyndwpgm
usermod -g users kyndjrrv
usermod -g users kyndjagd
usermod -g users kyndpaml
usermod -g users kyndlrpt

# === SAP ===
usermod -g users kyndyrch
usermod -g users kyndmxac

# === WME ===
usermod -g users kyndvaeb
usermod -g users kyndjtrc
usermod -g users kyndwapw

# === ACTIVACIONES ===
usermod -g users kyndlrpt
usermod -g users kyndpaml
usermod -g users kyndascy

# === CIBERSEGURIDAD ===
usermod -g users kyndkpoa 
usermod -g users kyndwccl
usermod -g users kyndjavc
usermod -g users kyndmaom